services:
  - type: web
    name: torn-property-manager
    env: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: TORN_API_KEY
        sync: false   # set manually in Render dashboard
