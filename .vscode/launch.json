{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug .NET (Linux) - dotnet run",
      "type": "coreclr",
      "request": "launch",
      "program": "dotnet",
      "args": [
        "run"
      ],
      "cwd": "${workspaceFolder}",
      "stopAtEntry": false,
      "console": "integratedTerminal"
    }
  ]
}
